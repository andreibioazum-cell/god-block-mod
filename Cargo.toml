 [package]
name = "cubic_battle"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
macroquad = "0.4"

[package.metadata.android]
package_name = "com.fast.cubic"
label = "Cubic Battle"
build_targets = ["aarch64-linux-android"]
min_sdk_version = 21
target_sdk_version = 30
